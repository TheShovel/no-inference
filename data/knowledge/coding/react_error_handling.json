[
    {
        "q": [
            "handle API errors in React",
            "React API error handling",
            "how to handle API errors in React",
            "error handling in React applications",
            "react error handling api",
            "handle errors in react",
            "react fetch error handling",
            "react api error handling example",
            "how to handle API errors in a React application",
            "react error boundary api"
        ],
        "a": "To handle API errors in a React application, you typically use try/catch blocks with async/await or .catch() with Promises, combined with React state management to show error feedback to users:\n\n```jsx\nimport { useState } from 'react';\n\nfunction DataFetcher() {\n  const [data, setData] = useState(null);\n  const [error, setError] = useState(null);\n  const [loading, setLoading] = useState(false);\n\n  async function fetchData() {\n    setLoading(true);\n    setError(null);\n    try {\n      const response = await fetch('/api/data');\n      if (!response.ok) {\n        throw new Error(`HTTP error! status: ${response.status}`);\n      }\n      const result = await response.json();\n      setData(result);\n    } catch (err) {\n      setError(err.message);\n    } finally {\n      setLoading(false);\n    }\n  }\n\n  if (loading) return <div>Loading...</div>;\n  if (error) return <div>Error: {error}</div>;\n  return <div>{data ? JSON.stringify(data) : 'No data'}</div>;\n}\n```\n\nFor more robust error handling, you can use React Error Boundaries to catch rendering errors or create a custom hook like `useFetch` with built-in error handling. Axios also provides interceptor-based error handling that can centralize error responses. Always handle both network errors and HTTP error status codes (4xx, 5xx) separately, and provide user-friendly error messages rather than raw error objects."
    }
]
