[
    {
        "q": [
            "fibonacci function python",
            "fibonacci sequence with memoization",
            "python fibonacci function",
            "fibonacci recursive memoization",
            "calculates the fibonacci sequence using memoization",
            "write a python function that calculates fibonacci",
            "fibonacci numbers python function",
            "write a function in Python that calculates the Fibonacci sequence using memoization",
            "fibonacci memoization python",
            "fibonacci sequence python"
        ],
        "a": "Here is a Python function that calculates the Fibonacci sequence using memoization:\n\ndef fibonacci_memo(n, memo=None):\n    if memo is None:\n        memo = {}\n    if n <= 1:\n        return n\n    if n in memo:\n        return memo[n]\n    memo[n] = fibonacci_memo(n-1, memo) + fibonacci_memo(n-2, memo)\n    return memo[n]\n\ndef fibonacci_iterative(n):\n    if n <= 1:\n        return n\n    a, b = 0, 1\n    for _ in range(2, n + 1):\n        a, b = b, a + b\n    return b\n\n# Example\nfor i in range(10):\n    print(f'F({i}) = {fibonacci_memo(i)}')\n\nMemoization reduces time complexity from O(2^n) to O(n). The iterative version uses O(1) space. Python handles arbitrarily large Fibonacci numbers with its built-in big integers."
    }
]
